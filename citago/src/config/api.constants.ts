/** All public endpoints are versioned under this prefix. */
export const API_PREFIX = 'api/v1';

export const SWAGGER_PATH = 'api/docs';

/** Request body cap: no endpoint needs more than this. */
export const JSON_BODY_LIMIT = '100kb';
