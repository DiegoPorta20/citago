/** Where a booking came from, for "where do our appointments come from?" analytics. */
export enum AppointmentSource {
  Manual = 'MANUAL',
  WhatsApp = 'WHATSAPP',
  App = 'APP',
  Other = 'OTHER',
}
